# Where the Medusa backend is reachable from the console SERVER (not the browser).
MEDUSA_BACKEND_URL=http://localhost:9000

# Optional: shown in the apply form so sellers see their future URL.
SELFKART_STOREFRONT_BASE_DOMAIN=localhost
